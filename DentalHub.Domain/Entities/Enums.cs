
public enum CaseStatus
{
    Pending,
    InProgress,
    Completed,
    Cancelled
}

public enum RequestStatus
{
    Pending,
    Approved,
    Rejected
}

public enum SessionStatus
{
    Scheduled,
    Done,
    Cancelled
}
